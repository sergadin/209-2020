В файлах "server.h", "server.cpp", "main_server.cpp", "client.py" изложена работа
сервера с векторами в векторном пространстве V размерности N.

Сервер ("server.h") умеет:
1)Хранить размерность V;

2)Добавлять новый вектор по запросу клиента с помощью функции add_vector с единственным аргументом - указателем на вектор;

3)Искать ближайшие K векторов к данному с помощью функции search_for_nearest_vectors, где 1 аргумент - данный вектор, 2 - число K; 

4)Подавать информацию о себе в определенном порядке: размерность V, мощность V, количество подключений, порт сервера, 
адрес сервера) с помощью функции get_server_info без параметров;

5)Удалять векторы, добавленные определённым клиентом с помощью функции clear_client_vector  с единственным параметром - id пользователя;

6)Разбирать запрос пользователя с помощью функции parsing с 1 аргументом - поданная пользователем строка, 2 - id пользователя;

7)Читать сообщения от клиента для дальнейшего разбора (п.6) с помощью функции ReadFromClient с аналогичными в п.6 аргументами;

8)Отправлять сообщения клиенту с помощью функции WriteToClient с аналогичными в п.6 аргументами;

9)Подключаться к нескольким пользователям (работа изложена в "main_server.cpp").


Клиент ("client.py") умеет:


1)Подключаться к серверу;

2)Отправлять серверу запросы(сообщения);

3)Получать от сервера сообщения.

(Все это было написано примерно за сутки в период с 28.09  по 29.09) F/29/09/20
